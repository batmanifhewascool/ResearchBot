This repository contains a research bot made using groq and serper, which crawls the web for the query given and compiles the results found from multiple sites onto a single markdown file.
The user can specify the depth of the search as a parameter by appending --d <number> to the search query
